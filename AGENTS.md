Run tests and static analysis tools on every code change, but not when only comments or documentation are changed.

Always adapt the documentation for the agents' best understanding. When a new agent is used to develop or the agent loses its context, they must be able to catch up through the documentation.

This project may be modified from other devices at all times, so I recommend always fetching, syncing, and pulling the main branch before starting to read the code (or create your branch from the main). If necessary, stash modifications before, then pop and apply after.

Update the language translation file (i18n.js), as needed, including placeholders, help text, and everything on the screen.

Project context:
#church-finder
Web  & app to help connect young people to the community of believers around Montreal and region.
#Terminology
**Every term used in the backend must be canonized. That is, one term for everything that refeers to that process or concept.
my project is not working. The database isn't connecting properly. The current appscript code is stored in src/services/google-sheets-backend.gs. Every test I do, I make a new deployment, update the link in config.js and delete all browser data. I believe there are terminology conflicts in my progect. I may have altered some names I shouldn't and not altered others that I should have.
-The concept of a church, an event host, organizer, leader, gathering host, the group hosting a meeting, or similar, will be called "host", except if it is in a place where the user can read (frontend), in that case, it may have any of these aliases according to their context.
-The same principle goes for the concept of a meeting, small group, gathering, event, service, church service, bible study, worship night, prayer night, community work, fellowship, outings, evangelisms, etc. They all will be called an "event", unless it is somewhere the user can read (frontend).
-Lastly, the same principle goes for the concept of Suggest an update, report an error, when there is a misinformation on an event or on a host info and the system allows the user to report it or send it for a review, update suggestions, request an info update, suggestion, any terms similar to these that may refeer to the same process. They'll be called "report" as in report an issue so that the admins or hosts may review and apply if pretinent. There may be other occurences of "suggestion" that does not refeer to this process of reporting an error, in those cases they may remain as suggestion or whatever other pertinent name they have in the back end (but always canonized), if the name is intuitive for their purpose.
-The concept of the person who accesses the website (or the app) to find a church or event is a common user, general user, or the person looking for a church.
-The concept of ADMs or admins are only for 3 people: the developers, the founder of Youth Montreal, and his CO founder only.
#Requisites
I need a website alongside its mobile app (for Android and IOS) that has a home page (landing); A page where the user can look for what is happening in Montreal and region in regards to church meetings that they can join. This section must have a map with the gathering places plotted in it where the user can click for more details and start navigation (that may redirect to their navigation maps app or google maps website); and A section where they can contact us via email.
The first section is the About Us, where they can read the reason why we exist.
The second section is the church finder, where they can switch between 2 different views. Users can look for events or for hosts. The user's current GPS position is read to set the center of the map as a default filter with a maximum range distance from that address. The map will show the user's current position as a black pin on the map. The map view will show the hosts according to the filters. Whenever an user selects a host, they'll see the selected pin in red. And whenever it is an ADM viewing the map, they'll also see a pin in red referring the host they're modifying. That is, everytime there's a pin on the map in context, it should be red, then after the focus leaves it, it gets its original color back. The calendar view shows the events happening. The calendar view has 3 exhibition options. The daily option will show the events happening today, according to the filters, but the user can also navigate (previous, today, next) to other days (obviously, the day they're visualizing must be visible in the interface). The weekly view is similar, except that it shows all the events of the week grouped by days of the week and their numbers (with the same navigation for previous, current, and next weeks). The monthly view has a agenda style layout where all the days of the month is shown with small references to events within the squares that represent days in the agenda (containing event title and host name).
The third section is visible only for hosts and admins. This section is where they can review change host requests submitted, and either accept them, modify and apply them, or refuse them. Hosts can only see reports concerning their own organization or their events. ADMs can see all the reports and the requests to be a host separately (with filters).
The fourth section is only visible to ADMs. This is where the Hardening Tools are located as well as the Audit log.
The fifth section is the Contact us section, where users can send an email to the admins. There's also a button here to apply to become a host.